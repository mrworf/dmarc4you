Read `AGENTS.md` and `docs/IMPLEMENTATION_PLAN.md`, then do the following:

1. Identify the next smallest end-to-end vertical slice that advances the product without skipping architecture constraints.
2. Name the exact files that should be created or edited.
3. List the acceptance criteria for that slice.
4. List the tests or validation steps needed.
5. Do **not** write code yet. Wait for approval after presenting the plan.

If the user typed extra text after this command, treat it as additional scope or priority guidance.
