Read `AGENTS.md`, `docs/FRONTEND_AND_DASHBOARDS.md`, and `docs/API_V1.md`.

Then implement the frontend slice requested in the current chat while following these rules:

- preserve the plain HTML/CSS/JavaScript SPA approach
- keep URL state bookmarkable
- do not move authorization into client-only assumptions
- keep viewer behavior temporary-only for filter changes
- update docs if route behavior or dashboard semantics change
- finish by summarizing files changed and validations run

Use any extra text after this command as the target frontend slice or screen name.
